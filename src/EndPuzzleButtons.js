
export default FinishButtons = ({ submit, isCorrect, clearPuzzle, puzzle, instanceId, time }) => {
    let buttons = (<div>
        <button onClick={clearPuzzle}>Clear Grid</button>
        <br />
        <button onClick={submit}>Next Puzzle</button>

        <br />

    </div>)
}